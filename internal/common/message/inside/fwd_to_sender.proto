syntax = "proto3";

package message2;
option go_package = "./inside";

message Message {
  uint32 to = 1;  // 要发送给哪个用户
  string msg_id = 2;  // 消息的ID
  bytes protobuf = 3; // 消息内容
  uint32 type = 4;    // 消息的类型
}